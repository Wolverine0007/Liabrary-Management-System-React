{
  "buildCommand": "cd frontend && npm install && NODE_OPTIONS=--openssl-legacy-provider npm run build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }
}